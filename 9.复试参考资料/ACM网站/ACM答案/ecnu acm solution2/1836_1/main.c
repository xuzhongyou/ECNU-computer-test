#include <stdio.h>
#include <stdlib.h>
/*需要建立数据结构struct Node
其中包含存储数据类别int type  1为数组 0为运算符
还有一个存储数据的int number
还有一个存储运算符的char oper
每次输入数值或者改变位置上面的数值的时候都需要修改type 每次读取数值的时候都需要先判断type的值在读取。
其中用到的栈，存储输入数据的数据都用此数据结构表示，可以不用链表。
*/

int main(int argc, char *argv[]) 
{
	system("pause");	
	return 0;
}
