#include <iostream>
#include <stdio.h>
#define N 10
using namespace std;
char pattern[N],str[N];
bool dfs(int x,int y)
{
    if(pattern[x]=='\0'&&str[y]=='\0')
        return true;
    if(pa)
}
int main()
{
    cout << "Hello world!" << endl;
    return 0;
}
